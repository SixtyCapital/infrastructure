---
- hosts: localhost
  vars:
    type: t2.large
    keyname: infrastructure-shared
    sg: sg-4a28e12d
    name: development
    deploy_env: dev
  roles:
  - { role: deploy_ec2, nodename: "{{ name }}",  number: "{{ count }}" }

- hosts: dev
  remote_user: ubuntu
  roles:
    - put-admin-key
    - docker
